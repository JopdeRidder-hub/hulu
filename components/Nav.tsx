import { NextPage } from "next"

interface Props {
    title: string;
    Icon: any;
  }

const Nav: NextPage<Props>= (props) => {
    const { } = props
  return (
    <div>Nav</div>
  )
}


export default Nav
